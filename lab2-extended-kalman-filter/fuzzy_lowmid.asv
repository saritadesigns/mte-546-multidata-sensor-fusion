function [mu_original] = fuzzy_lowmid(val,low,mid)
if val < low
    mu_original = 1;
elseif val > low && val < mid
    mu_original = (mid-val)/(mid-low);
elseif val > mid
    mu_original = 0;
end
end

